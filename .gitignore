# cached source downloads and build artifacts — regenerate with pipeline/run.py
data/raw/
data/build/

# app/public/canon.db IS committed: it ships with the app so CI can deploy
# without re-crawling sources. Refresh via `python pipeline/run.py && cp
# data/build/canon.db app/public/canon.db`.

node_modules/
app/dist/
__pycache__/
.env
